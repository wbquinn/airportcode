#
# Build steps.
#
steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - airport-code-lookup
  - --region=europe-west1
  - --source=.
  - --trigger-http
  - --runtime=nodejs16
timeout: 300s